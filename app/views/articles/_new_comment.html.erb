<% if signed_in? %>
  <h4 class="commentes">Add a comment</h4>
  <%= form_for([@article, @article.comments.build]) do |f| %>
    <p>
      <%= f.label :body, "Comments" %><br>
      <%= f.text_area :body %>
    </p>
    <!--p>
      <%#= f.label :rating %><br>
      <%#= f.text_field :rating, class: "rating" %>
    </p-->  
    <p>
      <%= f.submit "Submit" %>
    </p>
  <% end %>
<% end %>