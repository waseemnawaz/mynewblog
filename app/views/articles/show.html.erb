
<div class="container anchor" id="showz">
<p id="notice"><%= notice %></p>

<h1 class="titles">
  <%= @article.title %>
</h1>

<p class="date"><%= @article.created_at.strftime("%B, %d, %Y") %></p>

<h5 class="articlebody">
  <%= @article.body %>
</h5>
<p>
  <%= image_tag @article.image_url, class: "img-responsive showpic" %>
  <%#= @article.image_url %>
</p>

<!--p class="date">
	Submitted <%= time_ago_in_words(@article.created_at) %> Ago
</p-->

</div>
<%= render 'new_comment' %>
<%= render 'comments' %>
<%= will_paginate @comments %>
<% if signed_in? && current_user.admin? %><%= link_to 'Edit', edit_article_path(@article) %><% end %> |
<%= link_to 'Back', articles_path %>
